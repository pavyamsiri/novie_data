[general]
name = "Grid"

[versions.3-0-0]
[versions.3-0-0.attrs]
name = { dtype = "str", default = "UNKNOWN" }
extent = { dtype = "f64", default = "1.0" }
disc_scale_mass = { dtype = "f64", default = "1.0" }
disc_scale_length = { dtype = "f64", default = "1.0" }

[versions.3-0-0.datasets]
projection_xy = { dtype = "f32", shape = "(num_bins, num_bins, num_frames)" }
projection_xz = { dtype = "f32", shape = "(num_bins, num_bins, num_frames)" }
projection_yz = { dtype = "f32", shape = "(num_bins, num_bins, num_frames)" }
flat_projection_xy = { dtype = "f32", shape = "(num_bins, num_bins, num_frames)" }

[versions.3-0-0.fields]
num_frames = { type = "int", value = "num_frames" }
num_bins = { type = "int", value = "num_bins" }


[versions.4-0-0]
[versions.4-0-0.attrs]

[versions.4-0-0.datasets]
projection_xy = { dtype = "f64", shape = "(num_bins, num_bins, num_frames)" }
projection_xz = { dtype = "f64", shape = "(num_bins, num_bins, num_frames)" }
projection_yz = { dtype = "f64", shape = "(num_bins, num_bins, num_frames)" }
flat_projection_xy = { dtype = "f64", shape = "(num_bins, num_bins, num_frames)" }
completeness = { dtype = "b8", shape = "(num_frames,)", default = "one" }

[versions.4-0-0.fields]
